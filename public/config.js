/**
 * Optional: set when frontend is on Cloudflare Pages and API runs elsewhere (e.g. Railway).
 * Leave empty when the Node server serves both HTML and API (same origin).
 * Example for Pages + Railway: window.ND_API_BASE = 'https://your-app.up.railway.app';
 */
window.ND_API_BASE = '';
