# =============================================================================
# AutoElite / NightDrive — Environment variables
# =============================================================================
# Copy to .env and fill in values. No third-party bridge: server talks directly
# to MarketCheck (and optionally eBay). For production, set vars on your host
# (e.g. Vercel/Railway env dashboard or server .env).
# =============================================================================

# -----------------------------------------------------------------------------
# Required for cars to show (local + production). This key = high-volume inventory (50k+ cars).
# Variable name must be exactly MARKETCHECK_API_KEY (server reads this only).
# -----------------------------------------------------------------------------
MARKETCHECK_API_KEY=vwLpKZlT6jHbU4rOErXYm39MT0y6yUcx

# Auto.dev Vehicle Listings API (optional). When MarketCheck fails, real listings from Auto.dev are used first (1000 calls/month on Starter).
# Get key: https://auto.dev/signin → API Keys. Docs: https://docs.auto.dev/v2/products/vehicle-listings
AUTO_DEV_API_KEY=

# -----------------------------------------------------------------------------
# Required only in production (server will not start if missing when NODE_ENV=production)
# -----------------------------------------------------------------------------
NODE_ENV=development
ADMIN_TOKEN=
ALLOWED_ORIGINS=

# Local dev: leave ALLOWED_ORIGINS empty (same-origin only) or set:
#   ALLOWED_ORIGINS=http://localhost:1335
# Production (e.g. nightdrive.store on Cloudflare):
#   ALLOWED_ORIGINS=https://nightdrive.store
#   or with www: ALLOWED_ORIGINS=https://nightdrive.store,https://www.nightdrive.store

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------
PORT=1335

# When behind reverse proxy (Nginx, Caddy, cloud LB) with HTTPS
TRUST_PROXY=1
SSL_ENFORCED=1
CANONICAL_HOST=nightdrive.store
PUBLIC_BASE_URL=https://nightdrive.store

AUTO_DEV_API_KEY=sk_ad_fMwlLQPQf4_PifjayNh0VdGu
# eBay API (optional — both or neither). When MarketCheck quota is exhausted, home + inventory use real eBay listings instead of demo.
EBAY_ENVIRONMENT=sandbox
EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=

# Internal corpus endpoint (optional)
INTERNAL_CORPUS_KEY=

# Lead durability (serverless): POST leads to this URL so you don't lose them on ephemeral disk.
# Payload: { type: 'contact'|'newsletter', payload: {...}, ts: ISO date }
# Use Zapier/Make/CRM webhook, or your own endpoint.
LEAD_WEBHOOK_URL=
# When set, requests are signed with X-Webhook-Signature: sha256=<HMAC-SHA256(body, secret)>. Verify on receiver.
LEAD_WEBHOOK_SECRET=

# Account request confirmation email (no blank — required to send verification/confirmation to users).
# Resend: https://resend.com — create API key, then set NOTIFICATION_FROM_EMAIL to your sender (e.g. "NightDrive <noreply@nightdrive.store>" or "NightDrive <you@example.com>" after verifying domain).
RESEND_API_KEY=
NOTIFICATION_FROM_EMAIL=NightDrive <onboarding@resend.dev>
